name: Deploy

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    name: Deploy to EC2 VPS
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_SSH_HOST }}
          username: ${{ secrets.VPS_SSH_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_SSH_PORT || 22 }}
          # The web image's `next build` alone takes ~9–10 min on the small 2-core VPS; the
          # action's default command_timeout (10m) cut it off mid type-check. Give it headroom.
          command_timeout: 30m
          script: |
            # Without this, a failed `git pull` (e.g. a dirty working tree on the VPS) is
            # ignored: the build runs on the OLD code and the job still reports success.
            set -e
            cd ${{ secrets.VPS_DEPLOY_PATH }}
            git pull origin main
            docker compose up -d --build
            docker image prune -f
            docker builder prune -f
