fix(web): resolve telegram sync, ui bugs, and media handling

- Fix permanent delete silently failing in postgres by using correct row objects
- Convert uploaded webp images to jpg automatically to avoid unsupported extensions
- Hide tag display in UI when item details panel is inactive
- Implement soft-delete for folders so they move to trash instead of remaining
- Add missing confirmation dialog for restoring items from the trash bin
- Fix db_ops.py folder resolution to correctly use 'IS NULL' syntax in postgres
- Hide web dashboard link on inline bot keyboards if URL is localhost
- Pass TELEGRAM_API_URL to web container for correct local telegram routing
